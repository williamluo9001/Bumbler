<div class="indexbody" >
	<div class="slideDown">
		<h1 class="pulse"><i><u>New Bumbles!</i></u></h1>
		<div class="perPost">
			<% Post.last(10).reverse.each do |x| %>
			<p><%= 'Title: ' + x.title %><br>
				<%= 'By: ' + x.user.name %>
				<% end %>
			<br><br>
	
		</div>
	</div>

</div>
